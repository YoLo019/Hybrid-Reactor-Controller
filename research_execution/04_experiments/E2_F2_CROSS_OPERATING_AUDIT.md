# E2-F2 跨工况配置与逐射线括区 Runner 审计

审计状态：`PASS（2026-08-28）`

## 配置与身份

- 配置：`research_execution/04_experiments/configs/e2_f2_cross_operating_slow_v1.json`
- 配置 SHA-256：`98B3C08719A647559C3979B5BE867706017CFE9C65CFCAA5A8911D8FB3013053`
- Runner：`4_4/flexibility/run_e2_f2_cross_operating.py`
- Runner SHA-256：`949CBCC8F6A5D062467F2D37C7C1E2B12893A84A19C0BD36F24A1B2C0562D43D`
- 核心代码包 SHA-256：`C931CE2936BF1A3C2A3EF3060AC34CF48CEBF133953EB8A2B5CE438F65DAA3B1`
- F1 中心案例不并入 F2 聚合；F1 runner 不修改。

## 范围审计

- 9 个运行点：功率 `0.8/0.9/1.0 p.u.` × SOC `0.2/0.5/0.8`；
- 3 个慢频点：q0.99、q0.95、q0.90；
- 2 个冻结基线控制器：PID、MPC；
- 4 个相位：`0、π/2、π、3π/2`；
- 全矩阵 `216` 条射线，单 q0.99 波次 `72` 条射线；
- 输入域固定为 `D_ref/net_load_reference`，`dt=0.5 s`，恢复尾段 `180 s`；
- 粗网格固定为 `0、0.024548310554290048、0.0544、0.0982、0.1322、0.2168、0.3 p.u.`。

## Runner 审计

1. 每条射线独立执行粗网格，分别识别强迫阶段物理标签 `forcing_stage_safe` 与联合恢复标签 `joint_valid_for_boundary` 的首次安全—失效括区。
2. `precheck` 模式只做逐射线括区审计；`refine` 模式对两层分别二分到 `0.01 p.u.`，不把恢复完成判据用于强迫阶段二分。
3. 每条射线保留中心安全、端点、首次违规时间、主动约束、求解失败、恢复完成和非星形标签。
4. 父进程按 wave 维度加锁、可恢复地并行启动子进程，并在报告齐全后执行身份一致性与完整性聚合。
5. 发生代码或配置身份漂移时拒绝运行，不覆盖已冻结的 E2-F1 见证。

## 静态验收

- Python 编译检查通过；
- 配置校验：`pass=true`；
- 全矩阵展开：`216` 条射线；
- q0.99 展开：`72` 条射线；
- 项目回归测试：`11/11` 通过。

结论：q0.99 的 72 条预检波次可在本配置和 runner 身份下启动；预检完成后，只有通过逐射线中心安全、括区完整性、身份一致性和求解器门，才进入 q0.95/q0.90 或 F2 全量细化。
