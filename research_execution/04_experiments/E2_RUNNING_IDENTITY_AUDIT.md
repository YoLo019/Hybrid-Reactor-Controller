# E2运行中代码身份审计

**状态：V1 CLOSED / FORMAL AGGREGATION BLOCKED；V2 RE-RUNNING**  
**只读快照：2026-08-24 00:10:48；处置更新：2026-08-24 23:47**

## 发现

`e2_f1_center_slow_mid_wave_v1`的8条射线现已退出。对其case/summary做只读分组发现3种代码身份组合，计数分别为44、5、4；指标源身份也已跨组变化，因此该目录不能作为单一代码身份的正式波次整体聚合。

本文件记录分组事实而不维护逐case清单。v1保持为诊断资产，不覆盖或补写；正式修复重跑已在`runs/e2_f1_center_slow_mid_wave_v2/`启动。

## 原因与处置

E2并行器在worker空闲后新建子进程；共享运行源文件若在波次期间变化，后启动的子进程会记录新的代码身份。v1不删除、不覆盖，保留为吞吐和搜索范围诊断，但不得形成正式E2-F1结论；v2期间冻结共享代码文件。

正式修复重跑完成后应：

1. 冻结一套E2代码与指标源，禁止同波次期间修改共享文件；
2. 在新的versioned输出目录完整重跑中频波次，而不是在混合目录内补case；
3. 聚合前要求全部射线、case和summary代码身份唯一；
4. 只有新目录通过完整性、身份一致性和结构门后，才能进入E2-F1证据线。

本审计没有读取或改写NPZ数值，没有停止E2，也没有修改E2 runner、配置、模型或指标源。
