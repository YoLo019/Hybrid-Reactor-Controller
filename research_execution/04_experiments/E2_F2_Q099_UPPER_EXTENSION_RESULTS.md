# E2-F2 q0.99 右删失上界定向扩展结果

状态：`PASS（2026-08-29）`

## 1. 目的与边界

初始 q0.99 预检覆盖72条射线。9条 `P=0.8`、控制器为 `MPC` 的射线在注册搜索上界 `0.3 p.u.` 内全部安全，形成右删失，不能直接进入边界细化。本次只扩展这9条射线的搜索上界，不改动原始预检目录、不复用 E2-F1 案例，也不改变 F2 正式配置的研究身份。

目标射线索引为：`[1, 3, 5, 9, 11, 13, 17, 19, 21]`。

## 2. 执行契约

- 基础配置：`configs/e2_f2_cross_operating_slow_v1.json`
- 扩展配置：`configs/e2_f2_q099_mpc_upper_extension_v1.json`
- 基础聚合：`runs/e2_f2_cross_operating_q099_precheck_v1/e2_f2_q0.99_precheck_aggregate.json`
- 扩展 runner：`4_4/flexibility/run_e2_f2_q099_upper_extension.py`
- 新增上界序列：`0.35、0.4、0.5、0.6 p.u.`
- 单射线停止规则：首次出现 `forcing_stage_safe: true → false` 后停止；联合恢复标签并列记录，不反向改变强迫边界。
- 复用规则：只复用 F2 q0.99 预检中 `≤0.3 p.u.` 的案例；E2-F1 案例复用为 `false`。

## 3. 机器结果

目标扩展聚合：`runs/e2_f2_q099_mpc_upper_extension_v1/e2_f2_q0.99_mpc_upper_extension_aggregate.json`

| 项目 | 结果 |
|---|---:|
| 目标射线 | 9/9 |
| 新增案例 | 9/9（每条均只需 `0.35 p.u.`） |
| 强迫括区 | 9/9 为 `[0.3, 0.35] p.u.` |
| 联合括区 | 9/9 为 `[0.3, 0.35] p.u.` |
| 强迫阶段非星形 | 0 |
| 求解失败 | 0 |
| 恢复阶段新增物理违规 | 0 |
| 目标扩展门 | `true` |

`0.35 p.u.` 案例的首次失效均由 `neutron_power_pu` 上界触发；最大中子功率约为 `1.12458—1.12460 p.u.`，超过约束上限 `1.1 p.u.`。这说明原来的 `0.3 p.u.` 右删失是搜索域过短，而不是射线没有失效。

## 4. 72 条合并复核

合并聚合：`runs/e2_f2_q099_mpc_upper_extension_v1/e2_f2_q0.99_precheck_extended_aggregate.json`

- `status=complete`，72/72 射线齐全，缺失0、重复0；
- `precheck_gate_pass=true`；
- 中心不安全0、强迫括区缺失0、强迫阶段非星形0、求解失败0、恢复阶段物理违规0；
- 联合恢复非星形仍为 `[25,33,41]`，只作为联合层诊断保留，不改变强迫层门判定；
- 基础配置、核心代码和 F2 runner 身份保持不变。

## 5. 下一步

q0.99 预检现已具备完整强迫阶段括区。下一步对72条射线执行独立的强迫阶段/联合恢复双层细化二分；随后按同一配置身份启动 q0.95 与 q0.90 波次。
