# q0.99 最高慢频波次 v2：代码来源冻结

本目录是对 `e2_f1_center_slow_high_wave_v1` 的同身份修复重跑。v1 波次内部身份一致，但使用旧代码包；为形成正式跨频比较，v2 使用与 q0.95 v2、q0.90 相同的冻结代码身份。

统一代码身份：`C931CE2936BF1A3C2A3EF3060AC34CF48CEBF133953EB8A2B5CE438F65DAA3B1`。

波次期间不修改 E2 正式运行所依赖的 runner、模型、参数、指标和 schema 文件。只有 8 条射线完成、身份唯一、聚合通过后，才将 v2 纳入三频点正式比较。

执行见证：保留的唯一并行编排器为 PID `31036`（4 个 worker，2026-08-27 00:24:32 启动）。同目录曾误启动一组重复编排器 PID `21972`，已在结果完成前停止；其配置与代码身份相同，不作为独立运行计数。

聚合验收：q0.99 v2 已完成 8/8 射线聚合；缺失0、重复0、中心不安全0，8份 ray summary 与 92 个 case 对通过可读性复核。正式聚合文件为 `e2_f1_center_slow_high_wave_aggregate.json`。
