# 已替代

本目录用于检查首次向量参考实现的默认兼容性。MPC RMSE仅变化约`7.9e-9`，但数学结果哈希不一致，因此按严格回归标准判定不合格。

根因是默认基线也从标量CVXPY参数切换为向量参数，改变了OSQP数值装载路径。代码已修正为：默认基线保留原标量QP，只有启用参考预览时使用向量参数。合格兼容结果位于`../e2_frequency_crossover_v3_compat/`。
